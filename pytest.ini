[pytest]
testpaths = tests

addopts =
    -v
    --tb=short
    --strict-markers

markers =
    integration: marks tests that require mounted datasets and a live GROQ_API_KEY
    slow: marks tests that perform real LLM inference calls (skip in CI)

minversion = 7.4
log_cli = false
log_level = WARNING
